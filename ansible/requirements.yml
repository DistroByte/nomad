---
# Pinned so a fresh `ansible-galaxy install -r requirements.yml` reproduces the
# tested toolchain; bump deliberately, not as a side effect of reinstalling.
collections:
  - name: ansible.posix
    version: 2.2.2
  - name: artis3n.tailscale
    version: 1.2.1
  - name: community.docker
    version: 5.3.0
  - name: community.general
    version: 13.4.0
  - name: community.crypto
    version: 3.4.0
