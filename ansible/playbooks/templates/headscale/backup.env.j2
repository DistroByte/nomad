# Managed by ansible/playbooks/headscale.yaml — do not edit on the host.
# Deliberately NOT in Consul KV: this host must be restorable with the
# cluster dead.
RESTIC_REPOSITORY={{ headscale_restic_repository }}
RESTIC_PASSWORD={{ headscale_restic_password }}
AWS_ACCESS_KEY_ID={{ restic_b2_key_id }}
AWS_SECRET_ACCESS_KEY={{ restic_b2_application_key }}
HEARTBEAT_TOKEN={{ gatus_heartbeat_token }}
