---
# Installs Tailscale on all nomad nodes and connects them to headscale.
#
# The control plane runs off-cluster as a Docker Compose stack on worker
# (141.147.74.4), so every headscale CLI call goes over SSH into that container
# — not `nomad alloc exec`. See docs/Headscale.md.
#
# Prerequisites:
#   1. headscale is serving at https://headscale.dbyte.xyz (curl -sI .../health)
#   2. A headscale user and reusable pre-auth key have been created:
#        ssh ubuntu@141.147.74.4 'sudo docker exec headscale headscale \
#          --config /var/lib/headscale/config.yaml users create distro'
#        ssh ubuntu@141.147.74.4 'sudo docker exec headscale headscale \
#          --config /var/lib/headscale/config.yaml preauthkeys create \
#          --user 1 --reusable --expiration 90d'
#   3. The key is stored in Ansible vault:
#        ansible-vault encrypt_string '<key>' --name vault_tailscale_authkey \
#          >> ansible/group_vars/all.yaml
#
# Route approval is handled by the second play below, not by hand. It matters
# that it is automated: `tailscale_args` carries --reset, so a re-register
# leaves routes advertised but unapproved, and `approve-routes` REPLACES the
# node's whole approved set rather than adding to it — approving a subnet on a
# node that is also an exit node silently revokes the exit route unless every
# desired route is passed in the same call. The play always computes the full
# union from host_vars, which is what removes that footgun.
- name: Install and connect Tailscale
  hosts: tailscale
  become: true
  gather_subset:
    - '!hardware'

  pre_tasks:
    # ORDER AND `reload: false` BOTH MATTER HERE. Enabling
    # net.ipv6.conf.all.forwarding makes the host a router, and the kernel
    # responds by purging every RA-derived default route
    # (rt6_purge_dflt_routers) and ignoring subsequent RAs unless accept_ra is
    # 2, "overrule forwarding".
    #
    # On the home nodes that is not cosmetic. The line is DS-Lite with the IPv4
    # tunnel disabled by the ISP, so the SLAAC address and the RA default route
    # are the *only* transit out of the house: lose them and there is no IPv6,
    # therefore no headscale, therefore no tailnet, therefore no IPv4 egress
    # either. A run of this playbook took the home nodes off the network exactly
    # that way on 2026-09-09.
    #
    # `reload: true` was the trigger: it runs `sysctl -p`, which rewrites every
    # value unconditionally — including forwarding=1 when it was already 1 —
    # so an otherwise no-op run still purged the default route. `sysctl_set`
    # applies the live value on its own and `state: present` persists it, so
    # the reload was never needed.
    - name: Overrule forwarding's effect on router advertisements
      # Must precede the forwarding task: it is what lets the node re-accept an
      # RA after forwarding purges the route it is currently using.
      ansible.posix.sysctl:
        name: net.ipv6.conf.all.accept_ra
        value: "2"
        sysctl_set: true
        state: present
        reload: false

    - name: Enable IP forwarding for subnet routing
      ansible.posix.sysctl:
        name: "{{ item }}"
        value: "1"
        sysctl_set: true
        state: present
        reload: false
      loop:
        - net.ipv4.ip_forward
        - net.ipv6.conf.all.forwarding

    - name: Wait for the IPv6 default route before touching anything else
      # The home nodes have no other transit, and the next task the role runs
      # is apt. Without this check a purged default route surfaces as an apt
      # task hanging for minutes with nothing to explain it.
      when: inventory_hostname in groups['home']
      ansible.builtin.command:
        argv: [ip, -6, route, show, default]
      register: tailscale_v6_default
      until: tailscale_v6_default.stdout | trim != ''
      retries: 12
      delay: 10
      changed_when: false
      failed_when: false

    - name: Fail clearly if IPv6 transit is gone
      when:
        - inventory_hostname in groups['home']
        - tailscale_v6_default.stdout | default('') | trim == ''
      ansible.builtin.fail:
        msg: >-
          No IPv6 default route on {{ inventory_hostname }} after 120s, so this
          node has no transit at all (DS-Lite here carries no IPv4). Check
          `rdisc6 -1 <iface>` for a router advertising SLAAC, and
          `sysctl net.ipv6.conf.all.accept_ra` for the value 2. To restore it by
          hand: `ip -6 route add default via <router-link-local> dev <iface>`.

  roles:
    - artis3n.tailscale.machine

- name: Approve the advertised routes in headscale
  hosts: headscale
  become: true
  gather_facts: false

  tasks:
    - name: Read the registered nodes
      ansible.builtin.command:
        argv: [docker, exec, headscale, headscale, --config, /var/lib/headscale/config.yaml, nodes, list, --output, json]
      register: headscale_nodes
      changed_when: false

    - name: Work out what each node should have approved
      # The full set, derived from the same host_vars that decide what the node
      # advertises: anything absent here is revoked by the approve call below.
      ansible.builtin.set_fact:
        headscale_route_plan: >-
          {{
            headscale_route_plan | default([]) + [{
              'host': item,
              'name': hostvars[item].tailscale_hostname,
              'routes': (
                (hostvars[item].tailscale_advertise_routes | default('') | split(',') | select | list)
                + (['0.0.0.0/0', '::/0'] if hostvars[item].tailscale_exit_node | default(false) else [])
              ),
            }]
          }}
      loop: "{{ groups['tailscale'] }}"

    - name: Approve the union of desired routes
      ansible.builtin.command:
        argv:
          - docker
          - exec
          - headscale
          - headscale
          - --config
          - /var/lib/headscale/config.yaml
          - nodes
          - approve-routes
          - --identifier
          - "{{ node.id | string }}"
          - --routes
          - "{{ item.routes | join(',') }}"
      loop: "{{ headscale_route_plan }}"
      loop_control:
        label: "{{ item.name }} -> {{ item.routes | join(',') }}"
      vars:
        # The trailing [{}] keeps `first` defined for a node that is in the
        # inventory but not yet registered, which is normal mid-rollout.
        node: >-
          {{ ((headscale_nodes.stdout | from_json)
              | selectattr('given_name', 'equalto', item.name) | list + [{}]) | first }}
      when:
        - node.id is defined
        # Revoking down to nothing would need `--routes ""`, which no host in
        # this estate wants; a node with no desired routes is simply left alone.
        - item.routes | length > 0
        - (node.approved_routes | default([]) | sort) != (item.routes | sort)
      changed_when: true
