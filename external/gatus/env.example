GATUS_BIND_ADDR=127.0.0.1

# openssl rand -hex 16
NTFY_TOPIC=homelab-CHANGE-ME-to-something-random

# Optional bearer token for a protected or self-hosted ntfy. Leave blank for
# ntfy.sh public (and keep the `token:` line in config.yaml commented out).
NTFY_TOKEN=

# openssl rand -hex 16
HEARTBEAT_TOKEN=CHANGE-ME
